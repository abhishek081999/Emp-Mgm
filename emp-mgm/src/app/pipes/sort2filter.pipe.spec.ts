import { Sort2filterPipe } from './sort2filter.pipe';

describe('Sort2filterPipe', () => {
  it('create an instance', () => {
    const pipe = new Sort2filterPipe();
    expect(pipe).toBeTruthy();
  });
});
