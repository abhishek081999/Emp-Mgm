import { InstructorStatusFilterPipe } from './instructor-status-filter.pipe';

describe('InstructorStatusFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InstructorStatusFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
