import { QuizstatusPipe } from './quizstatus.pipe';

describe('QuizstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new QuizstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
