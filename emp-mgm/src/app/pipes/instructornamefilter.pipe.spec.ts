import { InstructornamefilterPipe } from './instructornamefilter.pipe';

describe('InstructornamefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new InstructornamefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
