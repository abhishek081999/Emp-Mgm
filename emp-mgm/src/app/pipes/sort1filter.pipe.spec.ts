import { Sort1filterPipe } from './sort1filter.pipe';

describe('Sort1filterPipe', () => {
  it('create an instance', () => {
    const pipe = new Sort1filterPipe();
    expect(pipe).toBeTruthy();
  });
});
