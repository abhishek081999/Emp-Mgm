import { DiscussionSlicePipe } from './discussion-slice.pipe';

describe('DiscussionSlicePipe', () => {
  it('create an instance', () => {
    const pipe = new DiscussionSlicePipe();
    expect(pipe).toBeTruthy();
  });
});
