import { CourseStatusFilterPipe } from './course-status-filter.pipe';

describe('CourseStatusFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseStatusFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
